# ── Local config (contains your absolute paths — NEVER commit this) ──────────
config.yaml

# ── Pipeline outputs (large, regeneratable) ───────────────────────────────────
Pipeline_Output/
My_video/

# ── Data (videos and datasets live outside the repo, but guard just in case) ──
moxie_data/
*.avi
*.mp4
*.mov
*.mkv

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[codz]
*.so
*.egg-info/
dist/
build/

# ── Dependency lock files (you're using uv) ───────────────────────────────────
uv.lock

# ── Virtual environments ──────────────────────────────────────────────────────
.venv
venv/
.pixi
__pypackages__/
.python-version

# ── Testing and coverage ──────────────────────────────────────────────────────
.pytest_cache/
.coverage
htmlcov/

# ── Jupyter (for any exploratory notebooks) ───────────────────────────────────
.ipynb_checkpoints/

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

.env